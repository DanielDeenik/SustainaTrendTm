run = "cd frontend && python app.py"
language = "python"
entrypoint = "frontend/app.py"
hidden = false
persistent = true
wait_for_port = 5000
onPort = { action = "open", port = 5000, url_path = "/enhanced-strategy-hub" }
environment = { DEBUG = "false", FLASK_ENV = "development", HOST = "0.0.0.0", PORT = "5000" }
